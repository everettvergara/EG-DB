#pragma once

#include <string>
#include <ctime>

#include "column.hpp"
// #include "record.hpp"

namespace eg
{

    
    // struct user : public record
    // {
    //     column<std::string> name;
    //     column<std::string> passwd;
    //     column<time_t>      bday;
        

    //     auto bind_cols() -> void override final
    //     {
    //         // bind_col_ptr(name.get_ptr());
    //         // bind_col_ptr(passwd.get_ptr());
    //         // bind_col_ptr(bday.get_ptr());
    //     }

        

    // };
}