#pragma once

#include <fstream>
#include <memory>
#include <optional>
#include <string>

#include "column_attr.hpp"
#include "column.hpp"
#include "row.hpp"
#include "identity.hpp"

namespace eg
{
    class table
    {
    public:

        table(const std::string &name)
            // : name_(name), id_(name_)
        {

        }

    private:

        // std::string name_;
        // identity id_;

    };

}